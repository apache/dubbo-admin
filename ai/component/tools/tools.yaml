type: tools
spec:
  enable_mock_tools: false
  enable_internal_tools: true  # 启用内部工具（memory + RAG）
  enable_mcp_tools: false  # 禁用MCP工具
  mcp_host_name: "mcp_host"
  mcp_timeout: 30
  mcp_max_retries: 3
