package engine

// ResourceEngine is the component which list and watch the runtime infrastructure of resources, like kubernetes, docker etc.
// TODO need to define the interface and implement it
type ResourceEngine interface {
}
