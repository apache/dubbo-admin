# Documentation: https://docs.codecov.io/docs/codecov-yaml

codecov:
  branch: develop

coverage:
  status:
    project:
      default:
        branches:
          - develop

comment:
  layout: "reach,diff,flags,files,footer"
  behavior: default
  require_changes: no
  branches:
    - develop
